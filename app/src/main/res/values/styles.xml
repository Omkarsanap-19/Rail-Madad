<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomSwitchTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">#C41E3A</item>
        <item name="colorSwitchThumbNormal">@color/switch_thumb_unchecked</item>
        <item name="android:colorForeground">@color/switch_track_unchecked</item>
    </style>

    <!-- Custom Spinner Style -->
    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@drawable/spinner_background</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>

    <style name="MyProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateTint">@android:color/holo_blue_dark</item>
    </style>

</resources>